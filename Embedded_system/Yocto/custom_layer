1. Set the build environment: source poky/oe-init-build-env ; cd ..
2. Create a layer: bitbake-layers create-layer meta-[name]
3. Add the layer: Open `build/conf/bblayers.conf` and add [root]/meta-[name]
